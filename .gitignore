# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Data artifacts (downloaded / generated locally)
backend/data/

# Frontend
frontend/node_modules/
frontend/dist/

# Local configuration. .env.example is the committed reference; a real .env
# is machine-specific and never a deliverable.
.env

# Misc
.DS_Store
*.log
vite.config.ts.timestamp-*.mjs
/tmp/

# Working material, not deliverables. requirements.txt is the canonical
# dependency manifest; uv.lock files here only create a second source of truth.
uv.lock

# IDE / local tooling
.idea/
.jbeval/
